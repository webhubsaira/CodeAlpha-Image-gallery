
## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Create [.env.local](.env.local) and set `VITE_GEMINI_API_KEY` to your Gemini API key
3. Run the app:
   `npm run dev`
